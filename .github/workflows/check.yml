on:
  push:
    branches: ['main', '*.*.x']
  pull_request: ~

jobs:
  check:
    uses: typhoon-php/.github/.github/workflows/check.yml@main
    secrets: inherit
    with:
      phpstan: true
      test: false
